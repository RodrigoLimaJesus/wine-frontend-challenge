import IProducts from '../interfaces/products';

const mockProducts: IProducts = {
  page: 0,
  totalPages: 0,
  itemsPerPage: 0,
  totalItems: 0,
  items: [],
  personalItems: [],
};

export default mockProducts;
